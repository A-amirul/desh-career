import useTitle from "../../useTitle";

const TermCondition = () => {
	useTitle("Term & Condition");
	return (
		<div>
			<h2>Terms and Condition page Upcoming....</h2>
		</div>
	);
};

export default TermCondition;